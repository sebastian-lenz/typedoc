/**
 * A basic enum.
 */
export enum EnumA {
  /** Enum with an inferred value */
  a,
  b,
}

/**
 * A const enum
 */
export const enum EnumB {
  /** The first element of the enum */
  a = 1,
  b = 2,
}
